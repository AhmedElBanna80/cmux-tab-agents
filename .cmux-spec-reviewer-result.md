---
ticket: ISSUE-20
phase: spec-reviewer
status: APPROVED
implementer_sha: 43e03552da2801238275382ecd7996e6f0bafda5
---

## Verdict
APPROVED — spec fully met. All acceptance criteria verified, tests pass, no hook bypasses. Conservative heuristic with deterministic helper.

## What was requested

Implement a planner-side heuristic for safely skipping code-quality review on trivial diffs. Include:
1. `references/skip-heuristics.md` documenting when it's safe to skip
2. `scripts/should-skip-code-review.sh` helper script to automate the decision
3. Update `SKILL.md` with a note (optional, not mandatory)
4. Comprehensive test coverage
5. CHANGELOG entry

## What was built

- **`skip-heuristics.md`** (93 lines): Conservative heuristic requiring ALL three conditions: ≤30 changed lines, test/spec/markdown/changelog files only, spec-reviewer APPROVED with no concerns. Includes clear examples of when skipping is and is not safe.
- **`should-skip-code-review.sh`** (85 lines): Executable shell script taking `--worktree` and `--implementer-sha` flags. Checks diff size, file patterns, and spec-reviewer result. Exits 0 (skip safe) or 1 (skip not safe) with reason on stderr. Deterministic—no LLM judgment, just git + regex.
- **`SKILL.md`** (updated): "Per-task pipeline" section expanded (lines 77–91) with optional skip heuristic, usage example, and clear emphasis that skipping is never mandatory.
- **`test-should-skip-code-review.sh`** (244 lines): Comprehensive test suite covering trivial test-only diff (skip), large diff (don't skip), spec-reviewer with concerns (don't skip), markdown-only diff (skip). All 4 tests pass.
- **`CHANGELOG.md`** (new): Entry under `[Unreleased]` → `Added` describing the feature and key points.

## Missing requirements
None.

## Extra / unneeded work
None. Scope is tight and matches the request exactly.

## Misunderstandings
None. Implementation aligns with the task intent.

## Hook-bypass check

- ✓ No `--no-verify` in commit message
- ✓ No hook-bypass evidence in `git log 40d3e4a..43e0355`
- ✓ Single clean commit with proper timestamp and author
- ✓ No suspicious split commits
- Conclusion: **clean** — hooks were respected.

## Verification commands re-run

**Test suite:**
```bash
bash skills/cmux-tab-agents/scripts/test-should-skip-code-review.sh
```
Exit: 0 — all 4 tests passed.
- ✓ trivial test-only diff → should skip
- ✓ large diff → should NOT skip
- ✓ spec-reviewer with concerns → should NOT skip
- ✓ markdown-only diff → should skip

**Script executability:**
```bash
ls -la skills/cmux-tab-agents/scripts/should-skip-code-review.sh
```
- ✓ `-rwxr-xr-x` permissions (executable)

**Helper script spot check:** Verified `--worktree` and `--implementer-sha` flag handling, exit codes 0/1, stderr reason output. ✓

**Documentation:** All files present and correctly formatted. Heuristic is precise, conservative, and includes examples. ✓

All acceptance criteria verified.
