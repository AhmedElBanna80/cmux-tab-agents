---
ticket: ISSUE-37
phase: spec-reviewer
status: ISSUES_FOUND
implementer_sha: b1a4b00
---

## Verdict
ISSUES_FOUND: Missing test file `tests/test-prompt-prefix-caching.sh` referenced in acceptance criteria. Test cannot be run to verify spec compliance.

## What was requested
Implement copy-pastable focus shortcuts for surface refs in tab-agent push protocol:
- Boot sequence captures OWN_SURFACE via `cmux identify --no-caller`
- Push lines emit two-line format: status line + focus command
- Reporting contract documents the format
- SKILL.md documents planner convention
- All three seed prompts updated identically
- Line targets maintained (implementer ≤100, spec-reviewer ≤80, code-reviewer ≤80)
- No new {{...}} placeholders
- `tests/test-prompt-prefix-caching.sh` still passes

## What was built
Implementer added:
- Boot sequence line in all three prompts capturing OWN_SURFACE with graceful fallback
- 4-line if block in push protocol (all three prompts) sending focus command as separate line
- CHANGELOG.md with entry under "Added"
- SKILL.md section "Surface refs in tab-agent reports" (9 lines) with planner convention
- reporting-contract.md documentation of new two-line push format with example
- Test file: `scripts/dev/tests/test-focus-command.sh` (validates focus command format, not prompt caching)
- No changes to section markers, file structure, or template placeholders

## Missing requirements
- `tests/test-prompt-prefix-caching.sh` does not exist in the worktree. This file is listed as an acceptance criterion ("still passes"). The implementer claims in self-review that the test passes, but the test file cannot be found to verify this. Other ISSUE worktrees (17, 22, 23, 24) have this test file; it should exist here too. **BLOCKING ISSUE.**

## Extra / unneeded work
None identified. All code additions are within spec.

## Misunderstandings
The implementer created `scripts/dev/tests/test-focus-command.sh` which tests focus command JSON syntax and format (4 tests, all pass). This is a reasonable addition but is not the same as the `tests/test-prompt-prefix-caching.sh` mentioned in the acceptance criteria. The latter verifies prompt prefix consistency across template variable substitutions — a cache-safety invariant from ISSUE-29. The implementer may have confused or conflated these two tests.

## Hook-bypass check
- No `--no-verify` in commit message
- No grep matches for hook bypass
- Single clean commit: `b1a4b00 feat(ISSUE-37): add copy-pastable focus shortcuts for surface refs`
- **Conclusion: Clean — no hook bypass detected**

## Verification commands re-run
1. `bash scripts/dev/tests/test-focus-command.sh` — **PASS**: 4/4 tests pass (JSON validity, command format, push line format, surface numbers)
2. `git log main..HEAD --grep='no-verify' -i` — **PASS**: No results (no hook bypass)
3. `tests/test-prompt-prefix-caching.sh` — **FAIL**: Test file does not exist

## Code review of changes
- Boot sequence: Correctly captures OWN_SURFACE with double fallback (`2>/dev/null | jq ... || echo ""`)
- Push protocol: Uses shell-safe single quotes around JSON: `'{"surface_id":"${OWN_SURFACE}"}'` ✓
- Conditional send: Correctly checks `if [ -n "${OWN_SURFACE}" ]` before emitting focus line ✓
- SKILL.md convention: Clear, well-written, actionable ✓
- Reporting contract: Good example, explains two-line format clearly ✓
- CHANGELOG: Comprehensive description, references ISSUE-37, explains graceful fallback ✓
- No new {{...}} placeholders introduced (only existing {{PLANNER_SURFACE}} used) ✓
- No changes to section markers or file structure ✓

## Line target verification
Implementer claims:
- implementer prefix 65 lines ≤ 100 ✓
- spec-reviewer prefix 78 lines ≤ 80 ✓
- code-reviewer prefix 80 lines ≤ 80 ✓

Changes were minimal (5 lines added per prompt), so limits should be respected. Not independently verified but implementer's claim is credible given diff size.

## Summary
Implementation is **technically sound**: boot sequence gracefully captures surface ref, push protocol correctly emits shell-safe focus command on separate line, documentation is clear and complete, and no hook bypasses found. However, the acceptance criterion "`tests/test-prompt-prefix-caching.sh` still passes" cannot be verified because the test file does not exist in the worktree. This is a **blocking issue** that must be resolved before approval.
