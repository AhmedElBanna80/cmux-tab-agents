---
ticket: ISSUE-8
phase: implementer
status: DONE
branch: feat/ISSUE-8/v0.3.0-defaults-and-setup
last_commit: 9cff6f5
---

## Summary

Implemented ISSUE-8 end-to-end: v0.3.0 configurable model/effort defaults + /cmux-tab-agents:setup interactive setup command.

### Part A — CLI flag + layered defaults ✓

- Added `--effort <level>` flag to dispatch scripts (levels: low, medium, high, xhigh, max)
- Implemented `resolve_setting()` function in `_dispatch_common.sh` with full 5-step resolution:
  1. CLI flag (`--model` / `--effort`)
  2. Env var (`CMUX_TAB_AGENTS_DEFAULT_MODEL` / `CMUX_TAB_AGENTS_DEFAULT_EFFORT`)
  3. Per-repo TOML (`.claude/cmux-tab-agents.toml`)
  4. User-global TOML (`~/.claude/cmux-tab-agents.toml`)
  5. Unset (claude uses its own default)
- Added `read_toml_value()` helper for flat TOML parsing (no nested tables, no multi-line strings)
- Integrated resolved values into boot command (appends `--model` and `--effort` iff non-empty)
- All existing config keys preserved (worktree_base, branch_type_default, setup_command, ticket_pattern)

### Part B — /cmux-tab-agents:setup command ✓

- Created `commands/setup.md` at plugin root (triggers `/cmux-tab-agents:setup`)
- Self-contained instructions for Claude Code to implement interactive flow
- Guides user through: model selection, effort selection, save location, bonus per-repo settings
- Reads existing TOML, merges new keys, preserves unrelated settings
- Output confirms saved settings and resolved values

### Part C — Documentation & version bump ✓

- Updated `references/configuration.md`:
  - Added `default_model` and `default_effort` keys to "Supported keys"
  - Created "User-global config" section documenting `~/.claude/cmux-tab-agents.toml`
  - Added "Resolution order for defaults" section with full 5-step order
  - Updated CLI flag descriptions to reference resolution
- Updated `SKILL.md`: added one-liner pointing users to `/cmux-tab-agents:setup` for first-time setup
- Updated `references/divergences-from-upstream.md`: added entry #14 covering v0.3.0 feature, rationale, and backward-compatibility
- Version bumped 0.2.1 → 0.3.0 in `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`

## Tests

### Resolution order tests (test_dispatch_resolution.sh)
- ✓ Test 1: CLI --model overrides env/TOML
- ✓ Test 2: CLI --effort overrides env/TOML
- ✓ Test 3: Env var CMUX_TAB_AGENTS_DEFAULT_MODEL used when CLI empty
- ✓ Test 4: Env var CMUX_TAB_AGENTS_DEFAULT_EFFORT used when CLI empty
- ✓ Test 5: Per-repo TOML default_model used
- ✓ Test 6: Per-repo TOML default_effort used
- ✓ Test 7: User-global TOML default_model used when per-repo absent
- ✓ Test 8: User-global TOML default_effort used when per-repo absent
- ✓ Test 9: Empty when all layers absent (MODEL)
- ✓ Test 10: Empty when all layers absent (EFFORT)

**Result: 10/10 passing**

### Acceptance tests (test_acceptance.sh)
- All 25 existing acceptance tests pass (backward-compatibility verified)

### Linting (make lint)
- JSON validation: OK
- Prompt template lint: OK (all 3 templates clean)
- Result: 3 checks ran, 0 failures

## Files changed

```
 M .claude-plugin/marketplace.json (version bump)
 M .claude-plugin/plugin.json (version bump)
 A commands/setup.md (new slash command)
 A scripts/dev/tests/test_dispatch_resolution.sh (TDD tests)
 M skills/cmux-tab-agents/SKILL.md (setup command mention)
 M skills/cmux-tab-agents/references/configuration.md (docs)
 M skills/cmux-tab-agents/references/divergences-from-upstream.md (divergence #14)
 M skills/cmux-tab-agents/scripts/_dispatch_common.sh (resolve_setting, --effort flag)
```

## Self-review findings

- **TDD discipline**: All Part A code written to pass failing tests first. Tests watched for 5-step resolution order across all layers.
- **Backward-compatibility**: Unset env vars and missing TOML files yield identical behavior to v0.2.0. No breaking changes.
- **No hook bypass**: All commits went through pre-commit hooks successfully (no `--no-verify`).
- **Code quality**: Mirrors existing patterns in _dispatch_common.sh (argument parsing, flag construction). TOML parsing kept minimal as per spec (regex + sed, no external deps).
- **Documentation**: Comprehensive—configuration keys documented, resolution order spelled out, setup command integrated into SKILL.md, divergences tracked.
- **Test coverage**: 10 specific resolution tests covering CLI > env > per-repo TOML > user-global TOML > unset, plus 25 existing acceptance tests.

## Concerns / blockers / context needed

None. All acceptance criteria met, all tests passing, hooks clean, branch ready for spec review.

